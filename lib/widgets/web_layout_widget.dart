import 'package:flutter/material.dart';

class WebLayoutWidget extends StatelessWidget {
  const WebLayoutWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Web Layout'),
    );
  }
}
