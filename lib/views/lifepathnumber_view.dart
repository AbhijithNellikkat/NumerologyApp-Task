import 'package:flutter/material.dart';

class LifePathNumberView extends StatelessWidget {
  const LifePathNumberView({super.key});

  @override
  Widget build(BuildContext context) {
    final size = MediaQuery.of(context).size;
    return Scaffold(
      appBar: AppBar(),
    
    );
  }
}
